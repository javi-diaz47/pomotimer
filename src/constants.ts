import type { Time } from "./types";

export const ONE_SECOND = 100;
export const DEFAULT_INTERVAL_ID = 0;
export const DEFAULT_TIME: Time = { min: 1, sec: 0 };
export const END_TIME: Time = { min: 0, sec: 0 };
